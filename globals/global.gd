extends Node
class_name Global

enum StatType {
    GRIND,
    SPEED, # speed is in milliseconds
    TRICK,
}

enum ScoreType {
    CURRENT,
    FINAL,
}

enum TrickType {
    TRICK_Z,
    TRICK_x,
    TRICK_C    
}